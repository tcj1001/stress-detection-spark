# Raw Dataset

原始 Fitbit/Bellabeat CSV 数据不提交到 Git，避免公开健康记录标识并控制仓库体积。

运行分析前，请将以下文件放入本目录：

- `dataset_daily.csv`
- `dataset_heartrate_minute.csv`
- `dataset_hourly.csv`
- `dataset_minute.csv`
- `dataset_sleep_minute.csv`

也可以通过 `STRESS_DATA_DIR` 环境变量指定其他本地数据目录。
